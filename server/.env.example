PORT=3000
NODE_ENV=development
ROOM_TTL_MINUTES=30
RESUME_DEADLINE_SECONDS=60
MAX_ROOMS=50
SESSIONS_PATH=/home/duotalk/sessions

# ⚠️  NUNCA subir estas claves al repo — configurar en el VPS via docker service update
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
